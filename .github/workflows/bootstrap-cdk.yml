name: Bootsrap CDK

on:
  workflow_dispatch:

env:
  python_version: 3.13
  working_directory: hello-cdk

concurrency:
  group: bootstrap-${{ github.head_ref || github.ref_name }}

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    environment: dev
    runs-on: ubuntu-24.04
    env:
      AWS_REGION:: ${{ vars.AWS_REGION }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@v5

      - name: Setup tooling
        uses: ./.github/actions/setup
        with:
          python_version: ${{ env.python_version }}
          working_directory: ${{ env.working_directory }}
          aws_region: ${{ vars.AWS_REGION }}
          role_to_assume: ${{ vars.AWS_OIDC_ROLE }}

      - name: Print CDK version
        run: npx aws-cdk --version

      - name: Bootstrap CDK
        run: |
          cd hello-cdk
          npx cdk bootstrap aws://${{ vars.AWS_ACCOUNT }}/${{ vars.AWS_REGION }} --qualifier hello-app --ci
