---
workflow: GitHubFlow/v1
branches:
  main:
    mode: ContinuousDeployment
    regex: ^main$
    is-release-branch: true
    label: '' # stable on main
  pull-request:
    label: PullRequest{Number}
    increment: Patch
    regex: ''
